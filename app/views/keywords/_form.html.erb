<%= form_for(@keyword) do |f| %>
  <% if @keyword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@keyword.errors.count, "error") %> prohibited this keyword from being saved:</h2>

      <ul>
      <% @keyword.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :words %><br />
    <%= f.text_field :words %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%= select("keyword", "category_id", Category.all.collect {|c| [c.name, c.id]}, {:include_blank => '---'}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
    <%= submit_tag "Create and Add another", :name => "add-another" %>
  </div>
<% end %>
